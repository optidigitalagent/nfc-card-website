# Independent v13 acceptance readiness audit

Reviewer: separate `v13_acceptance_auditor` context. This is a bounded, read-only source/evidence audit while browser verification and final revisions are still in progress. No browser, test, application mutation, deployment, provisioning or external send was performed by this reviewer. **Final acceptance is withheld pending the gates below.** This is not an implementation score.

Applied local guidance: `[REDACTED_LOCAL_PATH]`, Project Brain index, vision, quality bar, director protocol, reference library and relevant durable human feedback; current workflow context; `.codex/skills/acceptance-auditor/SKILL.md` and `.codex/skills/siteagent-acceptance-audit/SKILL.md`. Read the v13 start/execution prompt, owner screenshot matrix and pinned About source map. The owner's scoped refinement and no-publication instructions control this task; generic new-site/publication workflow steps do not authorize deployment.

## Independently established

- The existing target is `runs/nfc-card-manual-v1`; the route map preserves all 24 accepted public routes and adds only `/about` and `/en/about`.
- Independently recomputed the recovery archive SHA-256 and every archived entry: archive matches `eb27c8544287783c8759566cfcac1585ba59ad0108dae5cb6c23cc37fc1a24b5`; all 142 recorded entries match, with no missing entry.
- Independently compared 32 protected source/template/data files against recovery, including every `server/` file plus pricing/content/model, review/admin client and redirect boundaries. No drift was found. This proves byte preservation, not runtime regression success.
- Baseline matrix has 64 rows across exactly 320, 360, 390, 430, 768, 1024, 1280 and 1440 px, with no recorded page/HTTP errors. All 592 native screenshot references exist. The earlier preview interruption is retained and explained rather than hidden.
- The task checkpoint explicitly says `accepted:false`, `publicationAuthorized:false`, phase `implementation-verification`; this is appropriate at this stage.
- Content/technical critic has documented source/provenance checks and a concrete CT-01 analytics finding with rendered-UA/EN closure. Visual critic has explicit native-image rechecks for the mobile decode symptom, About heading wrap and ending composition, while correctly reserving final matrix/gesture acceptance.
- The rollback helper defaults to dry run; resolves and bounds every target inside this NFC application; checks the recovery ZIP, original entry bytes and every current modified/added file before any mutation; restores only named original files and unlinks only exact named additions. It has no recursive deletion and no database, upload or service operation. Rollback has not been executed by this audit.
- Only Chromium is installed in the established Playwright inventory. Additional-engine coverage is conditional in v13. The final report may state that Firefox/WebKit were unavailable; it must not imply they passed.

## Current revision and evidence readiness

Observed generated-site revision during this audit: `aecb8ba17c05c7ef25549ed1d994762660f02ce90682c48ec30be93bda00bfe1` (full generated-tree digest, same sorted path/digest algorithm as the browser harness).

`files-changed.json` is an in-progress inventory, not a final seal. Nine entries had changed after its generation: `site/en/index.html`, `site/index.html`, `src/build.mjs`, `tests/test_v12_browser.py`, `src/about-view.mjs`, `site/about/index.html`, `site/en/about/index.html`, `tests/test_v13.py`, `tests/test_v13_browser.py`. This is expected while revisions/tests are ongoing. The rollback helper currently fails closed for these entries. Regenerate the inventory only after the final source/test/workflow changes; then run its verification-only dry run and bind the resulting inventory to final acceptance.

The initial non-browser log contains 424 passes and five failed responsive-image assertions. The targeted correction/recheck is documented, but this initial log cannot be called a complete passing final suite. The retained v13 browser attempt also records a navigation-wait failure. Require later successful evidence for all affected tests and the full applicable suite; do not silently treat initial failures as application failures or erase them.

## Blocking final acceptance gates

1. **Current revision binding.** Freeze the final authored source and generated output, compute both full-tree digests, and verify that all after-width reports share that generated revision before/after their run. Include source, test/helper, evidence and native screenshot hashes. A changed CSS/JS/secondary page invalidates earlier visual evidence. Do not infer coverage from an `index.html` hash alone.
2. **Complete browser/interaction evidence.** Eight-width bilingual homepage/PDP/order/About after matrix, required component/state screenshots, no overflow/clipping/broken assets/console failures, all thumbnails and modal interactions, pointer and actual touch gestures, focus containment/restoration, scroll lock/restoration, 1/2/3+ pricing paths, messenger default/selected/focus/error/disabled states, sticky overlap, 200% actual browser zoom and reduced motion. The zoomed touch recheck must prove panning without inadvertently selecting another image.
3. **Full non-regression suite.** Final successful existing and targeted tests, including durable persistence, server-authoritative pricing, idempotency, pending-only reviews, authenticated moderation, consent/two-photo publication requirements, public/private media boundary, durable save before notification attempt and no PII analytics. Logs must identify commands, environment, exit/result and the revision tested. Test notifications must remain intercepted/disabled; synthetic review fixtures must not become candidate public proof.
4. **Owner issue closure and screenshot-led independent verdict.** Build `REQUIREMENT_MATRIX.md` and `SCREENSHOT_ISSUE_MATRIX.md` mapping every v13 requirement and all four supplied issues to native before/after evidence and behavioral results. Final visual/UX/responsive/accessibility/anti-template critic must inspect current screenshots and close every critical/high finding with exact route, width, state, evidence and acceptance condition. Source-only closure does not replace gesture or pixel evidence.
5. **Final artifact set.** Complete `TEST_REPORT.md`, `BROWSER_QA_MATRIX.md`, `BEFORE_AFTER.md`, `CRITIC_FINDINGS.md`, `ACCEPTANCE_REPORT.md` and machine-readable revision-bound candidate/evidence index. Reconcile all artifact paths, counts, hashes and critic revisions. Refresh route/media/file inventory and rollback verification after final edits.
6. **Honest status and limitations.** Record local candidate readiness separately from explicit owner acceptance (`CANDIDATE_READY` versus `USER_ACCEPTED`). State local unthrottled performance observations as such, not field/production Core Web Vitals. Report any unverified real Railway/S3, owner-auth configuration, physical-device or real-notification behavior precisely. No deployment, push, domain changes, production provisioning or real customer sends are authorized.
7. **Durable workflow/recovery.** Update only the relevant NFC task checkpoint and reusable task-local feedback, preserving unrelated workflow/worktrees and historical accepted v12 evidence. Keep the snapshot and rollback instructions resolvable after acceptance. Independent final audit must verify no current-revision critical/high issue remains.

## Preliminary disposition

No new critical/high implementation or rollback defect was established by this bounded source review. Acceptance remains pending, because final runtime evidence, complete matrix and final revision seal have not yet been supplied. This disposition must not be represented as final approval or production readiness.
