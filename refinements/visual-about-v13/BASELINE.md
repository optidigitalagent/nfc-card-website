# v13 baseline and recovery

Target: `[REDACTED_LOCAL_PATH]`. This is the existing accepted NFC CARD application, with its own source, generated output, local server, PostgreSQL adapters and regression suite. No new starter or site was created.

The instruction source is `[REDACTED_LOCAL_PATH]`, its Project Brain/workflow and local skill set; the workspace root and NFC child have no AGENTS.md. Director/web-studio guidance was explicitly applied. Full path/hash provenance is in [skill-provenance.json](skill-provenance.json). The owner's explicit existing-site refinement and no-publish instructions override new-site/go/publication defaults.

Input ZIP SHA-256: `0371367b87d7a75b300de8058a87d0255355b773206eff9a7afbf1df2dd38b81`. All 10 v13 manifest entries and 64 nested v11 entries verified. The nested v11 is byte-identical to the accepted v12 source pack previously read in its declared order. [Input verification](INPUT_VERIFICATION.json).

Before source changes, the complete accepted source/public seal matched: source `c068736efe48c775f9298d7c2471e9d24e6b56b9f129439f799b4fd4b720db57`, public `61f760660ade2329446e71b025874a5f59c79bb9cae7805313d1e1be7aa95dec`. Recovery contains 142 source/output/test/helper/checkpoint files and no databases, secrets or uploads. ZIP `eb27c8544287783c8759566cfcac1585ba59ad0108dae5cb6c23cc37fc1a24b5`. [Recovery inventory](RECOVERY_SNAPSHOT.json).

The baseline records 64 bilingual route/width rows at 320, 360, 390, 430, 768, 1024, 1280 and 1440 px: home, standard PDP, branded PDP and order, with catalog/quote/direct/founder/lightbox/3+/messenger/ending states. [Native before matrix](before-matrix.json), [screenshots](screenshots/before). Long pages use native viewport sequences with recorded scroll coordinates; no stitched reconstruction is presented as browser evidence.

The parent Git working tree already contained unrelated untracked projects and this run. It was preserved without stage/reset/checkout/commit/push. Baseline and current raw Git status are retained in [integrity-check.json](integrity-check.json). V12 acceptance artifacts remain historical and immutable; all current test evidence is redirected into this v13 directory.

A repeated user request interrupted the local preview after the first width. The original connection-refused log was retained, the existing preview helper restored port 8765, and the remaining baseline widths resumed before implementation. This was local preview recovery, not deployment.
