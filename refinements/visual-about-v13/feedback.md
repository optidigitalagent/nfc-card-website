# Durable v13 project feedback

The owner approved the existing NFC CARD graphite/silver identity and v11 commerce/reviews/security architecture. Future work should preserve these and edit only the requested components. The full factual About narrative and approved founder-media sequence come from the pinned Antonov Digital/iADDS source; this does not authorize copying its visual skin or substituting product names indiscriminately.

Reusable acceptance rules from this task:

- Product media needs an asset-aware frame; a generic fixed media height can create conspicuous empty bands. Retain source proportions and essential product edges/message/disclaimer.
- Marketplace galleries must expose visible thumbnails on mobile as well as desktop. Modal image resolution, focus/scroll restoration, real touch swipe and horizontal panning while zoomed are separate checks.
- Compact native messenger controls need recognizable icons, brand colors, non-color selection/focus cues and correct error feedback. Correcting one field should not clear errors elsewhere or unlock a pending durable request.
- Final CTA and footer are one page-ending composition. Check the actual390×844 ending, both languages, and the complete footer rather than a cropped screenshot or an arbitrary height score.
- Shared legacy IDs are behavior contracts. A new About chapter must not reuse the homepage story initializer's ID.
- Copied component markup must respect the existing CSP. Put source-bound image aspect ratios in an external stylesheet; never relax CSP to admit inline presentation styles. Check browser console and height stability before accepting image captures.
- Decode visible media, recompute document height after the lazy-loading sweep and prove that a native full-page sequence reaches the real bottom. An initial scroll-height estimate is not evidence that every About chapter/footer was captured.
- Media can look correct after decode and still shift the page on entry. Reserve the approved predecode ratio explicitly when component CSS overrides intrinsic width/height hints, then test a deliberately delayed image response. Attribute layout shifts to concrete DOM rectangles; a passed route test or a fully loaded screenshot does not establish layout stability.
- Local candidate QA, owner visual approval, production provisioning, deployment and real notifications are separate states. Preserve immutable prior acceptance evidence and bind every final capture/test to current source/output digests.

This record is task-local and linked from the NFC run workflow checkpoint, following the existing refinement convention. Unrelated global SiteAgent queues, delivery state and other projects were not rewritten.
